spring:
  application:
    name: test-bot-01
  
  thymeleaf:
    cache: false

  datasource:
    url: jdbc:mysql://localhost:3306/tradingbot_db
    username: your_username
    password: your_password
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true